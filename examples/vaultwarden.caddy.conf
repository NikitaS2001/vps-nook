# Caddy site block for Vaultwarden. Copy to
# /opt/vps-nook/Caddyfile.d/vaultwarden.conf.
# If internal_domain_suffix is not "internal", use vw.<suffix> instead.
vw.internal {
    tls internal
    reverse_proxy vaultwarden:80 {
        header_up X-Real-IP {remote_host}
    }
}
